# Port targets — `winapi-util-kotlin` (driven by codex-rs demand)

> Generated by `scaffold/generate_port_targets.py` from `codex_external_usage.json`. Do not hand-edit. Re-run after rebuilding the usage data.

This is the **minimum subset** of `winapi-util` that codex-rs uses, and therefore the minimum subset that `winapi-util-kotlin` needs to port in order for codex-kotlin to compile and run. Symbols are sorted by codex demand (most-used first). Anything not listed here is not currently required by codex — defer it.

**Jira Initiative**: [COD-190] (rank `114`, tier `direct`, status `To Do`)

**Cargo package name**: `winapi-util`
**Rust module name (in `use` statements)**: `winapi_util`

## Summary

- **2** distinct symbols imported by codex-rs
- **2** total `use` statements
- **1** codex source files affected
- **1** codex member crates transitively depend on this

## Granularity caveat

This list is **import-level**, not call-site-level. A symbol appearing here means codex `use`'d it; the specific functions, methods, fields, or trait impls codex calls on the imported name are not enumerated. For an initial port, implement the symbol's full API as defined in upstream Rust. For a tighter minimum, drill into each codex source file listed below and grep for the imported name to find concrete invocations — the graph tells you exactly which files to grep.

## Symbols by codex demand (port these in order)

### `winapi_util::sysinfo::ComputerNameKind`

- **1 `use` site(s)** across **1 codex member crate(s)**: `codex-config`

Codex source locations (relative to `tmp/codex/codex-rs/`):

- `config/src/host_name.rs:7` (`codex-config`) — `use winapi_util::sysinfo::ComputerNameKind`

### `winapi_util::sysinfo::get_computer_name`

- **1 `use` site(s)** across **1 codex member crate(s)**: `codex-config`

Codex source locations (relative to `tmp/codex/codex-rs/`):

- `config/src/host_name.rs:9` (`codex-config`) — `use winapi_util::sysinfo::get_computer_name`
